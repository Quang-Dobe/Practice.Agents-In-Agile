# <Feature title> — overview plan trace

Simple words. This file tracks technical decisions: what we looked at, what we chose, and why.
Technical nouns are fine — this trace is *about* technical choices. No code, no file paths, no
identifiers. `<feature>.overview-plan.md` holds the design itself; nothing here is a plan, and no
agent builds from it.

Append-only. The Architect writes one row per row of the overview-plan's `Key technical decisions`
table at stage-2-overview. Later rows are added whenever an answer changes `overview-plan.md` or
`plan.md` (the human gate — see `/feature:implement`).

## Decisions

| # | Date | Decision about | Options looked at | Chosen | Why | What changed in the plan |
|---|---|---|---|---|---|---|
| 1 | <YYYY-MM-DD> | <the concern> | <option / option / option> | <the one we take> | <≤30 words — a reason, not an essay> | <the row, step, or section it shaped> |
